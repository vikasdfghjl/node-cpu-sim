# NODEJS CPU SIMULATOR

```bash
npm run dev
```

> run prometheus server

```bash
docker-compose up
```

> run grafana server

```bash
docker run -d -p 3000:3000 --name=grafana grafana/grafana-oss
```
